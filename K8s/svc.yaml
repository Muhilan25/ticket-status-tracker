apiVersion: v1
kind: Service
metadata:
  name: node-service
  namespace: node
spec:
  selector:
    app: nodeapp
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer
